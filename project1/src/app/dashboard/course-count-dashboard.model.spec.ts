import { CourseCountDashboard } from './course-count-dashboard.model';

describe('CourseCountDashboard', () => {
  it('should create an instance', () => {
    expect(new CourseCountDashboard()).toBeTruthy();
  });
});
